setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: nginx/Dockerfile
    api: backend/Dockerfile
run:
  api: "gunicorn openbeats_backend.wsgi:application --bind 0.0.0.0:$PORT"