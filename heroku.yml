setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: nginx/Dockerfile
    worker: backend/Dockerfile
run:
  worker: gunicorn api.wsgi --bind 0.0.0.0:$PORT